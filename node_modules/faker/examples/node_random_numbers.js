#!/usr/bin/env node

var faker = require('../index');

for(var i = 0; i < 100; i++) {
//  console.log('a',faker.random.number({ max: 2 }));
console.log('a',faker.random.number({ min: 22, max: 33 }));

//  console.log(faker.address.streetAddress(3));
}

var arr = [1, 2, 3, 4, 5];

//console.log(faker.random.array_element(arr));
